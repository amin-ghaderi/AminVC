"""
Contracts package (Phase 1).

`app/contracts/` contains schemas/DTOs/interfaces that define stable boundaries.
It must not contain runtime lifecycle logic, persistence, orchestration, or engine calls.
"""

from __future__ import annotations

