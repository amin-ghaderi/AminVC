# Comma-separated browser origins allowed to call GET /agent/status.
# Production: your Vercel frontend origin only. Do not use *.
AMINVC_WEB_ORIGIN=https://YOUR-FRONTEND.vercel.app

# Optional. Platforms like Render inject PORT automatically.
PORT=8090
